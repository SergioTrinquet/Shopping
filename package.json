{
  "name": "shopping-app_api",
  "version": "1.0.0",
  "description": "Partie back-end du projet 'shopping-app' realisé avec Vue.js",
  "main": "index.js",
  "scripts": {
    "dev": "set NODE_ENV=development&& nodemon API/index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node API/index",
    "build": "npm install && npm install --prefix App && npm run build --prefix App"
  },
  "author": "Serge Trinquet",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.12.7"
  },
  "devDependencies": {
    "dotenv": "^17.0.1"
  }
}
